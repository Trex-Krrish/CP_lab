#include <stdio.h>
#include <conio.h>

void main(){
    float b,h;
    printf("Enter the value of B\n");
    scanf("%f",&b);
    printf("Enter the value of H\n");
    scanf("%f",&h);
    printf("The are of triangle is: %f",b*h/2);
}